# 更新日志

本文件记录`deepeval-article-audit`的行为、结果契约和兼容性变化。最新版本置顶。

## v1.1.0 - 2026-08-21（内容单元可视化修正，版本号不变）

### 变更

- 高亮 HTML 改为直接按预处理器生成的内容单元渲染；同一原始Markdown行中的多个句子不再合并为一个颜色块。
- 每个网页内容卡片显示文章行号、`unit_id`、事实/非事实状态和原子主张数量，并继续支持主张证据展开和状态筛选。
- 网页同时解释事实内容占比与Faithfulness主指标的不同分母，避免把颜色面积或文字长度误读为事实占比。

### 兼容性

- 不改变Faithfulness公式、事实/证据判断规则、prepared/judgments schema或`faithfulness_summary.md`导入主表。
- 不改变内容单元数量、原子主张数量或既有审计结果；本次只修正报告呈现层。

## v1.1.0 - 2026-08-21（报告呈现更新，版本号不变）

### 变更

- 在不改变 claim-level Faithfulness 公式、judgments schema、v0.5 严格证据范围或导入主表的前提下，明细、汇总和高亮 HTML 现在额外展示每个`articleUnit`是否属于事实内容。
- 新增事实内容占比、完全支持/部分支持/完全不支持/非事实单元诊断；该诊断明确不等同于 Faithfulness 或原文引用率。
- 高亮 HTML 展示完整文章内容，按内容单元状态着色，并提供事实、支持、不支持和非事实筛选；表格字段仍保留原子主张级核验。
- README 补充当前计算口径、`articleUnit`的确定性切分方式、事实单元聚合规则、报告层与导入层边界，并保留 v0.5 仅允许证据正文作为正向证据的限制。

### 兼容性

- 版本号保持`v1.1.0`；prepared与judgments继续使用`schema_version: 1.0`。
- `faithfulness_summary.md`原有导入主表列顺序和数值保持不变，内容单元诊断追加在主表之后。
- 8月20日加入的 v0.5 单一事实输入、证据正文范围和完整证据区间校验保持不变。

## v1.1.0 - 2026-08-20（兼容性修复，版本号不变）

### 变更

- `manage-article-knowledge v0.5`模式固定只接收当前`30_本篇知识库资料.md`一个事实输入；随文事实文件必须先由知识库流程合并进`30/35`。
- v0.5解析器改为只提取第1至第3节中`证据正文（供Faithfulness核验）`标题下的内容。直接写作事实、英文表达、数据展示表、大纲建议、生成控制和缺口处理不再进入prepared证据上下文。
- supported证据的起止行必须完整位于同一个可用证据块内，不能只让起始行碰到证据范围。
- v0.5交接不读取`35`作为retrieval context；Formal Claim仍由接收方使用`35`的哈希和证据行映射导入。

### 验证

- 测试固定件升级为当前六节写作素材包，覆盖中文事实、英文短事实、控制段排除和证据正文定位。
- 与`manage-article-knowledge v0.5`真实导入器完成端到端测试，验证同一证据区间可以确定性回写多个Formal Claim。

## v1.1.0 - 2026-08-19

### 新增

- 支持`manage-article-knowledge v0.5`标准文章文件`40_最终文章.md`和事实输入`30_本篇知识库资料.md`。
- 优先从`文章ID`或`Article ID`元数据读取文章编号，并拒绝显式ID与元数据冲突。
- 新增中文正文、中文句末标点和短事实单元支持，例如`IP67 certified.`不再因token过少被预处理器丢弃。
- 新增端到端测试，可选择调用v0.5真实`import_faithfulness.py`验证交接产物。

### 变更

- 抽出共享解析模块，使预处理和渲染阶段使用一致的正文、知识块、编码和路径逻辑。
- 知识块生成逻辑与v0.5导入器对齐；v0.5的禁止写入和未解决问题区段仍保留在prepared审计轨迹中，但不会进入正向候选证据。
- 渲染前重新读取当前文章和知识文件，校验prepared内容、schema、文章单元和知识块是否仍然有效。
- 引文校验改为原始Markdown连续子串校验，与v0.5导入器一致；不再只按去格式后的可见文本放行。
- 汇总表中的英文竖线改用HTML实体，避免v0.5导入器按列解析时错位。
- 明确v0.5交接只需要prepared JSON、judgments JSON和原始summary，并要求知识文件按prepared中的顺序传入。

### 兼容性

- prepared与judgments继续使用`schema_version: 1.0`。
- 原有明细Markdown、批量summary和交互HTML文件名保持不变。
- 旧写作输入文件仍可审核；本次更新主要保证新v0.5项目可稳定导入。

## v1.0.0 - 2026-08-17

- 首次建立DeepEval Faithfulness规则复现流程。
- 提供原子事实拆分、supported/unsupported判断、逐条Markdown明细、批量汇总、交互式HTML和JSON审计轨迹。
